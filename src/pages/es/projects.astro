---
import ProjectsCard from '../../components/sections/ProjectsCard.astro';
import IdentitySidebar from '../../components/shell/IdentitySidebar.astro';
import Nav from '../../components/shell/Nav.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getContact, getProfile, getProjects } from '../../lib/content/loaders';

const locale = 'es' as const;
const [profile, projects, contact] = await Promise.all([getProfile(locale), getProjects(locale), getContact(locale)]);
const currentPath = `${Astro.url.pathname}${Astro.url.search}`;
---

<BaseLayout
  locale={locale}
  currentPath={currentPath}
  title="Casos · Isaías Cordero"
  description="Casos de trabajo frontend con enfoque en arquitectura, performance y delivery."
  canonicalPath={currentPath}
  pageHeading="Casos"
>
  <Fragment slot="sidebar">
    <IdentitySidebar
      locale={locale}
      name={profile.data.name}
      headline={profile.data.headline}
      location={profile.data.location}
      email={contact.data.email}
      linkedin={contact.data.linkedin}
    />
  </Fragment>

  <Fragment slot="panel-tabs">
    <Nav locale={locale} currentPath={currentPath} />
  </Fragment>

  <ProjectsCard locale={locale} title="Casos de trabajo" projects={projects} />
</BaseLayout>
